## Scans

APIs: `inclusive_scan`, `exclusive_scan`, `stable_cumprod`.

### Operations
- `op='add'|'max'|'logsumexp'` supported for scans.
- `stable_cumprod(x, axis, logspace=True)` computes a numerically stable cumulative product.


