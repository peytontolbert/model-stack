## Scheduling and memory planning

APIs: `accum_steps`, `microbatch_plan`, `bucket_sizes`, `tensor_shards`, `estimate_bytes`, `activation_bytes`, `roofline`.

All are pure calculators returning suggested plans and estimates.


