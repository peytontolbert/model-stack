## regularization

Drop-path/stochastic depth and label smoothing/z-loss tools.

### APIs
- `drop_path`, `StochasticDepth`
- `z_loss_from_logits`, `label_smooth`, `grad_noise_std`
- Token/sequence drop: `build_tokendrop_mask`, `build_sequencedrop_mask`, `prune_topk_`, `magnitude_mask`


